name: Java CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 20
        uses: actions/setup-java@v2
        with:
          java-version: '20'
          distribution: 'adopt'
      - name: mvn -B clean package -D skipTests
        run: mvn -B clean package -D skipTests
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: samsla
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Publish to Docker Hub
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: samsla/site-backend:latest

      - name: Connect
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          script: |
            docker pull samsla/site-backend:latest
            docker rm -f site-backend
            docker run -d --name site-backend -p 8090:8090  samsla/site-backend